# Database URL — required for drizzle-kit migrations (local dev only)
# In production, DATABASE_URL is injected via Render environment variables
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME
