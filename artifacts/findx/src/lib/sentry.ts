/**
 * lib/sentry.ts (frontend) — no-op stub
 *
 * Sentry is intentionally not imported to keep the bundle clean.
 * Add real Sentry init here once VITE_SENTRY_DSN is configured.
 */

export function initSentry(): void {}
