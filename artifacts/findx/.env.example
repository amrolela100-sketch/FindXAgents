VITE_SUPABASE_URL=https://[PROJECT-REF].supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
# Production (Vercel): use relative path — routes via Vercel proxy to avoid CORS/CORP issues
VITE_API_URL=/api
# Local dev only: point directly to the API server
# VITE_API_URL=http://localhost:3000/api
