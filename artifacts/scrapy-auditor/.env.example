# Port the auditor listens on (default: 4000)
PORT=4000

# Internal API secret — set the same value in api-server .env as SCRAPY_AUDITOR_SECRET
# Leave empty to disable auth (not recommended in production)
SCRAPY_AUDITOR_SECRET=
